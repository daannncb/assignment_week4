//get user data and send to server

//TODO:::::::::::::::::::::::::::::::::::::::::::::
//select html element with user's input
//      add event
//      event handler will:
//      prevent default event.preventDefault();
//      create template for user's data
//      `INSERT INTO table $1 $2 $3
//      fill in template
// send data JSON to the server
//n.b. while in dev the server uses local host, but when deploying, this needs to be replace with the server deployed url

//TODO:::::::::::::::::::::::::::::::::::::::::::::
//get data from server AND display on page
//connect client to the specific server route that gets data from supabase
//insert into HTML; append child, "messages" section in the HTML
//      in some sort of loop, create new html elements to display each piece of data
//      and then append to DOM: overflow-y CSS; ABSOLUTE POSITIONING
